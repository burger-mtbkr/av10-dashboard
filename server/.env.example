# Marantz AV10 connection — update with your receiver's IP
AVR_HOST=192.168.1.100
AVR_PORT=23
AVR_HTTP_PORT=8080

# Server
SERVER_PORT=3001

# Polling interval (ms) for full HTTP status refresh as backup to telnet
POLL_INTERVAL=30000
